Initial commit recommendation:

```text
feat: add Reflex Index answer-capsule retrieval prototype

- add Vite + React + Tailwind browser demo
- compile documents into answer capsules in localStorage
- implement hybrid capsule scoring and confidence gate
- compare direct-hit latency against simulated RAG baseline
- document architecture, RAG baseline, and benchmark plan
```
